createUser:
  handler: src/index.createUser
  description: Create a new user
  events:
    - httpApi:
        method: POST
        path: /user
getUserInfo:
  handler: src/index.getUserInfo
  description: Get user info
  events:
    - httpApi:
        method: GET
        path: /user/{userId}
